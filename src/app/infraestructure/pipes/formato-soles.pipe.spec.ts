import { FormatoSolesPipe } from './formato-soles.pipe';

describe('FormatoSolesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoSolesPipe();
    expect(pipe).toBeTruthy();
  });
});
